Write a php program using function.


<?php
function multi(int $a, int $b) {
  return $a * $b;
}
echo multi(5, 10);
// since strict is NOT enabled "5 days" is changed to int(5), and it will return 10

//Write a php program using function.

?>